{
  "ENABLE": true,
  "id": "run402",
  "name": "Run402",
  "description": "Full-stack infrastructure for AI agents: Postgres database + REST API + user auth + file storage + RLS + static site hosting + image generation. Prototype tier is FREE.",
  "categories": ["infrastructure", "database", "hosting", "ai"],
  "serviceUrl": "https://api.run402.com",
  "tags": ["postgres", "database", "api", "auth", "storage", "hosting", "static-site", "image-generation"],
  "docs": [
    "https://run402.com/llms.txt",
    "https://run402.com/openapi.json"
  ],
  "endpoints": [
    {
      "method": "POST",
      "path": "/tiers/v1/prototype",
      "description": "Subscribe to prototype tier (FREE - uses testnet USDC to verify setup)",
      "pricing": {
        "type": "fixed",
        "amount": "0.10",
        "per": "subscription",
        "note": "Testnet USDC only - $0 real money"
      }
    },
    {
      "method": "POST",
      "path": "/tiers/v1/hobby",
      "description": "Subscribe to hobby tier (30-day lease, 1 GB storage, 5M API calls)",
      "pricing": {
        "type": "fixed",
        "amount": "5.00",
        "per": "subscription"
      }
    },
    {
      "method": "POST",
      "path": "/tiers/v1/team",
      "description": "Subscribe to team tier (30-day lease, 10 GB storage, 50M API calls)",
      "pricing": {
        "type": "fixed",
        "amount": "20.00",
        "per": "subscription"
      }
    },
    {
      "method": "POST",
      "path": "/projects/v1",
      "description": "Create a Postgres database project with REST API (requires SIWX auth)",
      "pricing": {
        "type": "free",
        "note": "Free with active tier subscription"
      }
    },
    {
      "method": "POST",
      "path": "/deployments/v1",
      "description": "Deploy a static site (requires SIWX auth)",
      "pricing": {
        "type": "free",
        "note": "Free with active tier subscription"
      }
    },
    {
      "method": "POST",
      "path": "/deploy/v1",
      "description": "Bundle deploy: database + migrations + functions + site + secrets + subdomain in one call",
      "pricing": {
        "type": "free",
        "note": "Free with active tier subscription"
      }
    },
    {
      "method": "POST",
      "path": "/subdomains/v1",
      "description": "Claim a custom subdomain like myapp.run402.com",
      "pricing": {
        "type": "free",
        "note": "Free with active tier subscription"
      }
    },
    {
      "method": "POST",
      "path": "/image/v1",
      "description": "Generate an image from text prompt (PNG output)",
      "pricing": {
        "type": "fixed",
        "amount": "0.03",
        "per": "image"
      }
    },
    {
      "method": "POST",
      "path": "/faucet/v1",
      "description": "Get free testnet USDC on Base Sepolia (0.25 USDC)",
      "pricing": {
        "type": "free",
        "note": "Rate limit: 1 drip per IP per 24h"
      }
    },
    {
      "method": "POST",
      "path": "/message/v1",
      "description": "Send feedback to Run402 team (requires SIWX auth)",
      "pricing": {
        "type": "free"
      }
    },
    {
      "method": "GET",
      "path": "/health",
      "description": "Health check with component status",
      "pricing": {
        "type": "free"
      }
    }
  ],
  "network": "eip155:8453",
  "currency": "USDC",
  "note": "Prototype tier is completely FREE (uses testnet USDC). Production tiers (hobby/team) use real USDC on Base mainnet or can be prepaid via Stripe credits."
}
